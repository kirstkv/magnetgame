services:
  - type: web
    name: magnetgame-backend
    env: node
    repo: https://github.com/kirstkv/magnetgame
    branch: main
    buildCommand: "npm install"
    startCommand: "npm start"
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
